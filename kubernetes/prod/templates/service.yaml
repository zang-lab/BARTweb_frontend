apiVersion: v1
kind: Service
metadata:
  name: {{ include "bartweb.fullname" . }}
  labels:
    {{- include "bartweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bartweb.selectorLabels" . | nindent 4 }}
